<?php
// Roundcube newmail_notifier plugin — rendered by mail-deploy. Enable
// desktop notifications (the user must still grant the browser permission
// prompt); nothing here is site-specific, so this file has no template
// placeholders to fill in.
$config['newmail_notifier_basic']   = true;
$config['newmail_notifier_sound']   = true;
$config['newmail_notifier_desktop'] = true;
